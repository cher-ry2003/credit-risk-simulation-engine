name: 'consumer_risk_engine'
version: '1.0.0'
config-version: 2

profile: 'consumer_risk_engine'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  consumer_risk_engine:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
